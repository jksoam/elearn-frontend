name: frantend
pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: 
    jobs:
      - job:
        steps:
        - task: Npm@1
          inputs:
            command: 'install'
        - task: Bash@3
          inputs:
            targetType: 'inline'
            script: 'npm run build'


     